html
css
     >units(px,%,vh,vw)
     //label,input,span (inline elements)
     //h1,div (block element)
     >inline element,block element
     >box models(margin,padding)
     >positions(relative,sticky,absolute,fixed,static:(it is default))
     >flex

Bootstrap
     >css framework
     >btn btn-primary|success|warning|danger|info|dark
     >getbootstrap
     >grid system(12 block)
     >xs(extra small), md(medium), lg(large) etc
     >nav bar
     >carousel
     >modal window
     >according
     >collapse
     >cards